[project]
name = "scripts"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk>=0.2.110",
    "python-dotenv>=1.2.2",
    "tzdata>=2026.2",
]
